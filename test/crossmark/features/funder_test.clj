(ns crossmark.features.funder-test
  (:require [clojure.test :refer :all]
            [crossmark.features.funder :as funder]))

(deftest ^:todo decorate-funder
  (testing "decorate-funder should set :has-funder and :funder fields"))
